/* magnetic.js — disabled, replaced with CSS hover */
/* The magnetic pull effect was removed for UX reasons.
   Hover states are handled purely in CSS via .btn:hover */
(function () {
  /* no-op: intentionally empty */
})();
